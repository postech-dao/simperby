use super::*;

pub(super) enum NetworkOperation {
    // TODO
}

pub(super) async fn run_network_task(
    _network: Box<dyn AuthorizedNetwork>,
    _node_state: Arc<RwLock<NodeState>>,
) {
    unimplemented!()
}
